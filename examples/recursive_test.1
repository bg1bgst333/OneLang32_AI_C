# 階乗
fact(n) {
    n <= 1 -> {
        <- 1
    } -> {
        <- n * fact(n - 1)
    }
}
= fact(5)
= fact(10)

# フィボナッチ
fib(n) {
    n <= 1 -> {
        <- n
    } -> {
        <- fib(n - 1) + fib(n - 2)
    }
}
= fib(10)
