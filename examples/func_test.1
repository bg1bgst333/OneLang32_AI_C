# 基本的な関数
add(a, b) {
    <- a + b
}
= add(1, 2)
= add(10, 20)

# 変数に代入
x = add(3, 4)
= x

# 引数なし関数
hello() {
    こんにちは
}
hello()

# 条件分岐を含む関数
max(a, b) {
    a > b -> {
        <- a
    } -> {
        <- b
    }
}
= max(5, 3)
= max(2, 8)
